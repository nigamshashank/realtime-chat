[phases.setup]
nixPkgs = ["python3", "make", "gcc", "g++"]

[phases.install]
cmds = ["npm install", "npm rebuild swisseph"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm start" 